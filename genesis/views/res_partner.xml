<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="genesis_partner_form" model="ir.ui.view">
        <field name="name">base.view_partner_form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">
                <header>
                    <button string="Créer employé" type="object" name="action_create_employee" attrs="{'invisible': ['|',('employees_count', '>', 0), ('company_type', '!=', 'person')]}"/>
                </header>
            </xpath>
        </field>
    </record>

    <record model="ir.actions.server" id="action_create_employee">
        <field name="name">Créer les employés</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="base.model_res_partner" />
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.action_create_employee()
        </field>
    </record>

</odoo>
